# Legacy copy — use `aegis-labs-site` for production

**Production** for [aegis-labs.pro](https://aegis-labs.pro) moved to the sibling repository **`aegis-labs-site/`** next to this project (self-contained app + `content/posts` + `components/simulations`).

This folder is kept for reference or local comparison only. Do not rely on it for deploys.

## Historical note

This app previously read posts from `../content/posts` and symlinked `components/simulations` to `../../components/simulations`. The standalone repo inlines those paths.
